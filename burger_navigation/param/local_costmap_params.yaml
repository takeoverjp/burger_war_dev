local_costmap:
  global_frame: odom
  robot_base_frame: base_footprint

  update_frequency: 10.0    # 計算資源が足りないときは低く？
  publish_frequency: 10.0   # 計算資源が足りないときは低く？
  transform_tolerance: 0.5  

  static_map: false  
  rolling_window: true
  width: 3
  height: 3
  resolution: 0.02          # 計算資源が足りないときは高く？
  
